% Código exacto da funcionalidade «Como usar» (copiado de templates/index.html,
% static/style.css e static/script.js). Colar no relatório em três blocos minted.

% -----------------------------------------------------------------------------
\subsection*{trecho de \texttt{templates/index.html} (botão + modal)}
% -----------------------------------------------------------------------------
\begin{minted}[breaklines]{html}
    <!-- Como usar -->
    <button type="button" class="help-nav-btn" id="help-btn" aria-haspopup="dialog" aria-controls="help-modal" aria-label="Como usar o Signify">?</button>

    <!-- Dictionary link - top right corner -->
    <a href="{{ url_for('dicionario.dicionario') }}" class="dict-nav-link">📓 Dicionário</a>

    <div id="help-modal" class="help-modal" role="dialog" aria-modal="true" aria-labelledby="help-modal-title" hidden>
        <div class="help-modal__backdrop" tabindex="-1"></div>
        <div class="help-modal__stack">
            <div class="help-modal__panel">
                <button type="button" class="help-modal__close" aria-label="Fechar">×</button>
                <h2 id="help-modal-title" class="help-modal__title">Como usar</h2>
                <p class="help-modal__intro">O Signify é uma aplicação que utiliza a câmara para reconhecer gestos e convertê-los em texto em tempo real.</p>
                <ul class="help-modal__list">
                    <li><strong>Câmara</strong> — Clique para ativar a <strong>câmara</strong>. O navegador vai pedir permissão — aceite para que o reconhecimento funcione.</li>
                    <li><strong>Letras/Palavras</strong> — Escolha o modo conforme o que pretende: soletrar com o alfabeto ASL ou reconhecer palavras em LGP.</li>
                    <li><strong>Letra atual / Palavra</strong> — Mostra o que está a ser reconhecido no momento.</li>
                    <li><strong>Texto completo</strong> — Apresenta a frase ou o texto que vai sendo construído.</li>
                    <li><strong>Limpar texto, Espaço e Apagar</strong> — utilize estes botões para apagar todo o texto, adicionar espaços ou apagar a última palavra.</li>
                    <li><strong>Dicionário</strong> — para ver exemplos de letras e de palavras (botão no canto superior direito).</li>
                </ul>
                <p class="help-modal__note">Esta aplicação é apenas para fins educativos, serve como apoio à comunicação e o reconhecimento de gestos pode não ser totalmente preciso, não substituindo um intérprete profissional.</p>
            </div>
            <p class="help-modal__tap" id="help-modal-tap">Clica em qualquer sítio para continuar</p>
        </div>
    </div>
\end{minted}

% Nota: o link do Dicionário está colado ao modal no ficheiro real; podes omitir as duas linhas do <a ... Dicionário> no relatório se quiseres só o ajuda.

% -----------------------------------------------------------------------------
\subsection*{trecho de \texttt{static/style.css} (estilos e animações do modal)}
% -----------------------------------------------------------------------------
\begin{minted}[breaklines]{css}
/* ? Como usar — top left (Dicionário stays top right) */
.help-nav-btn {
    position: fixed;
    top: 1.2rem;
    left: 1.5rem;
    z-index: 100;
    width: 2.5rem;
    height: 2.5rem;
    padding: 0;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    font-size: 1.15rem;
    font-weight: 800;
    line-height: 1;
    color: rgba(255, 255, 255, 0.92);
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
    cursor: pointer;
    transition: background 0.25s ease, border-color 0.25s ease, transform 0.2s ease;
}

.help-nav-btn:hover {
    background: rgba(255, 255, 255, 0.14);
    border-color: rgba(255, 255, 255, 0.35);
}

.help-nav-btn:focus-visible {
    outline: 2px solid rgba(255, 255, 255, 0.5);
    outline-offset: 2px;
}

.help-modal[hidden] {
    display: none !important;
}

.help-modal {
    position: fixed;
    inset: 0;
    z-index: 10000;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1.25rem 1.5rem;
    box-sizing: border-box;
}

.help-modal__backdrop {
    position: absolute;
    inset: 0;
    background: rgba(5, 12, 28, 0.65);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
}

.help-modal__stack {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    width: 100%;
    max-width: min(720px, 94vw);
    gap: 1rem;
}

.help-modal__panel {
    position: relative;
    width: 100%;
    max-height: min(88vh, 780px);
    overflow-y: auto;
    padding: 1.85rem 2rem 1.5rem;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.15);
    background: rgba(18, 32, 58, 0.92);
    box-shadow: 0 28px 72px rgba(0, 0, 0, 0.5);
}

.help-modal__close {
    position: absolute;
    top: 0.85rem;
    right: 0.85rem;
    width: 2.4rem;
    height: 2.4rem;
    border: none;
    border-radius: 10px;
    font-size: 1.55rem;
    line-height: 1;
    color: rgba(255, 255, 255, 0.7);
    background: rgba(255, 255, 255, 0.06);
    cursor: pointer;
    transition: background 0.2s ease, color 0.2s ease;
}

.help-modal__close:hover {
    background: rgba(255, 255, 255, 0.12);
    color: #fff;
}

.help-modal__title {
    font-size: 1.55rem;
    font-weight: 800;
    color: rgba(255, 255, 255, 0.95);
    margin-bottom: 1.15rem;
    padding-right: 2.75rem;
    letter-spacing: 0.04em;
}

.help-modal__intro {
    font-size: 0.95rem;
    line-height: 1.55;
    color: rgba(220, 230, 255, 0.9);
    margin: 0 0 1rem;
}

.help-modal__list {
    margin: 0;
    padding-left: 1.4rem;
    color: rgba(230, 238, 255, 0.88);
    font-size: 0.98rem;
    line-height: 1.65;
}

.help-modal__list li {
    margin-bottom: 0.85rem;
}

.help-modal__list strong {
    color: #fff;
    font-weight: 700;
}

.help-modal__note {
    margin-top: 1.15rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    font-size: 0.88rem;
    line-height: 1.5;
    color: rgba(255, 255, 255, 0.45);
    font-style: italic;
}

.help-modal__tap {
    margin: 0;
    flex-shrink: 0;
    text-align: center;
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    color: rgba(180, 220, 255, 0.85);
    animation: helpTapPulse 2s ease-in-out infinite;
}

.help-modal:not(.help-modal--splash) .help-modal__tap {
    display: none;
}

@keyframes helpTapPulse {
    0%, 100% { opacity: 0.65; }
    50% { opacity: 1; }
}

/* Stack (cartão + linha "clica…") — splash (1.ª visita) ou botão ? (mesmo efeito) */
.help-modal--splash .help-modal__stack,
.help-modal--drop .help-modal__stack {
    animation: helpSplashIn 0.75s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

.help-modal--splash.help-modal--leave .help-modal__backdrop {
    opacity: 0;
    transition: opacity 0.45s ease;
}

.help-modal--splash.help-modal--leave .help-modal__stack {
    animation: helpSplashOut 0.48s cubic-bezier(0.55, 0.06, 0.68, 0.19) forwards;
}

@keyframes helpSplashIn {
    from {
        transform: translateY(-125%);
        opacity: 0.35;
    }
    to {
        transform: translateY(0);
        opacity: 1;
    }
}

@keyframes helpSplashOut {
    to {
        transform: translateY(-125%);
        opacity: 0;
    }
}
\end{minted}

% -----------------------------------------------------------------------------
\subsection*{trecho de \texttt{static/script.js} (função \texttt{initHelpModal})}
% -----------------------------------------------------------------------------
\begin{minted}[breaklines]{javascript}
// Como usar (? modal) + entrada inicial (splash) a cair de cima
(function initHelpModal() {
    const btn = document.getElementById('help-btn');
    const modal = document.getElementById('help-modal');
    if (!modal) return;

    const backdrop = modal.querySelector('.help-modal__backdrop');
    const closeBtn = modal.querySelector('.help-modal__close');
    const stack = modal.querySelector('.help-modal__stack');

    function closeModal() {
        modal.hidden = true;
        modal.classList.remove('help-modal--splash', 'help-modal--leave', 'help-modal--drop');
        if (btn) btn.focus();
    }

    /** Abrir pelo botão ? — mesmo efeito de queda que ao carregar a página */
    function openModalFromButton() {
        modal.classList.remove('help-modal--splash', 'help-modal--leave', 'help-modal--drop');
        modal.hidden = false;
        void modal.offsetWidth;
        modal.classList.add('help-modal--drop');
        function onDropInEnd(e) {
            if (e.target !== stack || e.animationName !== 'helpSplashIn') return;
            stack.removeEventListener('animationend', onDropInEnd);
            if (!modal.hidden) modal.classList.remove('help-modal--drop');
        }
        if (stack) stack.addEventListener('animationend', onDropInEnd);
        if (closeBtn) closeBtn.focus();
    }

    function dismissSplash() {
        if (!modal.classList.contains('help-modal--splash') || modal.classList.contains('help-modal--leave')) {
            return;
        }
        modal.classList.add('help-modal--leave');
        let finished = false;
        function finish() {
            if (finished) return;
            finished = true;
            closeModal();
        }
        if (stack) {
            stack.addEventListener('animationend', function onEnd(e) {
                if (e.animationName !== 'helpSplashOut') return;
                stack.removeEventListener('animationend', onEnd);
                finish();
            });
        }
        setTimeout(finish, 600);
    }

    // Entrada: mostrar "Como usar" ao abrir o site
    modal.hidden = false;
    modal.classList.add('help-modal--splash');

    modal.addEventListener('click', function (e) {
        if (modal.hidden) return;
        if (modal.classList.contains('help-modal--splash') && !modal.classList.contains('help-modal--leave')) {
            dismissSplash();
            return;
        }
        if (e.target === backdrop) closeModal();
    });

    if (closeBtn) {
        closeBtn.addEventListener('click', function (e) {
            e.stopPropagation();
            if (modal.classList.contains('help-modal--splash') && !modal.classList.contains('help-modal--leave')) {
                dismissSplash();
            } else {
                closeModal();
            }
        });
    }

    if (btn) {
        btn.addEventListener('click', function (e) {
            e.preventDefault();
            e.stopPropagation();
            openModalFromButton();
        });
    }

    document.addEventListener('keydown', function (e) {
        if (e.key !== 'Escape' || modal.hidden) return;
        if (modal.classList.contains('help-modal--splash') && !modal.classList.contains('help-modal--leave')) {
            dismissSplash();
        } else {
            closeModal();
        }
    });
})();
\end{minted}
